import React from 'react';

/**
 * Legacy export: disclaimers were removed from in-app reflection UI.
 * Renders nothing; kept so stale Metro bundles / HMR do not crash on a missing module.
 */
export const LegalNotice: React.FC = () => null;
