# Legacy icon components

These components have no runtime consumer. They are retained only to trace
superseded Oneiros icon work and must not be imported by application code.

`OracleGlyph.tsx` is the retired moon-in-cup animation formerly exported from
the shared UI barrel. The remaining files are pre-current generated Insights
marks.
